apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tns-csi
  namespace: flux-system
spec:
  interval: 1h
  path: ./clusters/homelab/infrastructure/tns-csi
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: sources
    - name: cluster-secret-store
