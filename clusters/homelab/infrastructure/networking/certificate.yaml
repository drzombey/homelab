apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-nomans-dev
  namespace: networking
spec:
  secretName: wildcard-nomans-dev
  issuerRef:
    name: cloudflare
    kind: ClusterIssuer
  dnsNames:
    - "*.home.nomans.dev"
